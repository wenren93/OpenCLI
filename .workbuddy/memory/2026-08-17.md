# 2026-08-17

- OpenCLI `bilibili user-videos <uid>` 命令可用：抓取指定 UP 主投稿列表（`--limit` 最多 50、`--order pubdate/click/stow`、`--page` 翻页）。底层走 `/x/space/wbi/arc/search` + WBI 签名，需登录态（cookie 持久化于 `~/.opencli`，当前已登录账号 wenren88）。
- 抓取 UP 522693174（财经/炒股博主，标题含「光模块/抄底/科创50」，与闻人关注方向吻合）投稿 50 条成功，产物：`/tmp/bili_522693174.{txt,md,csv,json}`。
- 资料库保存受阻：资料库 skill 的 `mode=client` 需 `connect_open_platform` 换 token，但当前 WorkBuddy 框架下 `DeferExecuteTool` 转发时总把 `toolName` 字段也注入目标工具入参，而 `connect_open_platform` 的 schema 为 `additionalProperties:false`，导致换票参数校验恒失败。已落地 CSV 供用户在资料库网页端手动导入，待框架修复后再自动写入。
- 补充：翻页拉全该 UP 全部投稿共 113 条（page1=50 / page2=50 / page3=13，非之前误判的 250），按发布时间倒序合并去重，导出 `/tmp/bili_522693174_all.{csv,md}` 供入库。
